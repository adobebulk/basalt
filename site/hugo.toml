baseURL = "https://photos.ctsmith.org/"
locale = "en-us"
title = "Photos"
theme = "gallery"

[params]
  photographer = "Your Name"
  description   = "A photo gallery"
  # Number of columns in the project grid on the homepage (desktop)
  gridColumns   = 3
  # R2 public bucket custom domain. Images are referenced as
  # {assetsBaseURL}/<key>/<size>.<fmt>  e.g. .../iceland-2025/001/1200.avif
  # Hostname is still TBD — see DEPLOYMENT-PLAN.md §10.
  assetsBaseURL = "https://assets.ctsmith.org"

[build]
  writeStats = true

# NOTE: no [imaging] block anymore — Hugo no longer processes images at build.
# Variants are pre-baked into R2 by the admin (600/1200/2400 px, AVIF + JPEG).
